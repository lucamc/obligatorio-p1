class Sistema {
  listaCensistas = new Array();
  listaFormularios = new Array();

  existeObjetoUsuario(array, Unusuario) {
    for (let usuario of array) {
      if (array.usuario == Unusuario) {
        return true;
      }
      return false;
    }
  }
}
